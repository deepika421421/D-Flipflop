# D-Flipflop
D Flip-Flop is a sequential circuit that stores one bit of data based on the clock signal. It transfers the input **D** to the output **Q** on the active clock edge, making it useful for data storage and synchronization.
